Modules:
Interface.py (Entry point/Main TUI)
projects_manager.py (Gather the data for new ini files, forwards it for creating a new one or overwriting.)
config_manager.py (Loads ini files and/or overwrites them with ProjectsManager()'s help.)
version_logic.py (Creates/updates the changelogs of the projects)
opener.py (Cross-platform file and folder opening utility)

Planned:
cloud_backup.py (Backs up the changes into the users preferred cloud service)
===============================================================================
(* - not yet started)
Structure:

    	Assistant's TUI
  	           |
Projects     <-|->     Settings
    |         	          |
* x3 pinned	           Pin projects*
* Latest project       Program's memory ini
* New project
    |
Projects Manager
	|
* Enter new project info
* Change project info

* Cloud backup*    ------> Cloud Backup
				                |
			             1. Identifies the projects
			                cloud service.
			             2. Performs an automatic backup.
			             3. Opens the cloud service.

* Manage changelogs ------> Version Logic
				                |
			                 Update individual
			                 changelogs
